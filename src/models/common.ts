export enum Status {
  Active = 'active',
  Inactive = 'inactive',
}
export enum ContentType {
  post = 'post',
  link = 'link',
  audio = 'audio',
  video = 'video',
  form = 'form',
  image = 'image',
  file = 'file',
}
export enum FormFieldType {
  Text = 'text',
  Textarea = 'textarea',
  Number = 'number',
  Email = 'email',
  Date = 'date',
  Select = 'select',
  Radio = 'radio',
  Checkbox = 'checkbox',
}
